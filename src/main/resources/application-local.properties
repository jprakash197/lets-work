#Registering mysql Driver
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
# Connection url for the database "orchard_tracks"
spring.datasource.url=jdbc:mysql://localhost:3306/letswork_june_19?useSSL=true&serverTimezone=UTC
# Username and password
spring.datasource.username = root
spring.datasource.password = Welcome123

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL57Dialect
# Show or not log for each sql query
spring.jpa.show-sql = true
# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = update
#App properties
app.title=Let's Work
app.project=A Venue Booking Application
app.version=Local-3.0
app.environment=Local
spring.mvc.view.prefix =/WEB-INF/view/
spring.mvc.view.suffix =.jsp

# App Properties
app.jwtSecret=JyotiPrakash
app.jwtExpiration=120000

logging.level.org.springframework.mail=DEBUG

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=letswork.mindtree19@gmail.com
spring.mail.password=letswork123

# Other properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# TLS , port 587
spring.mail.properties.mail.smtp.starttls.enable=true
